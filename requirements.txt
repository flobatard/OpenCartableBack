# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
pydantic-settings[yaml]>=2.4.0

# Base de données SQL
alembic==1.13.1
psycopg2-binary==2.9.9
sqlalchemy>=2.0.0
asyncpg>=0.29.0

# LangChain & IA — noyau 1.x (client multi-provider confiné dans app/core/ai/)
langchain>=1.3,<2
langchain-core>=1.4,<2
langgraph>=1.2,<2                 # runtime agent (create_agent) — déjà transitif, pin explicite
langchain-text-splitters>=1.1,<2
# Partenaires alignés core 1.x — un package par provider du client IA
langchain-openai>=1.3,<2          # OpenAI + tout endpoint "openai_compatible" (Groq, vLLM, LM Studio…)
langchain-anthropic>=1.6,<2
langchain-google-genai>=4.2,<5
langchain-mistralai>=1.1,<2
langchain-ollama>=1.1,<2
langchain-huggingface>=1.2,<2
# Héritage conservé (décision actée) — 0.4.x = la ligne compatible langchain-core 1.x
langchain-community>=0.4,<0.5
sentence-transformers>=5.0,<6

# Base vectorielle
chromadb>=1.0,<2
langchain-chroma>=1.1,<2

# Observabilité IA — opt-in via LANGFUSE_* (voir app/core/ai/observability.py)
langfuse>=3.15

# Extraction de texte PDF (tool read_resource_pdf de l'assistant de cours) —
# pur Python, ARM64 OK
pypdf>=6,<7

# Crypto / JWT (validation des tokens Zitadel via JWKS)
pyjwt[crypto]>=2.9.0
cryptography>=42.0.0

# HTTP
httpx>=0.27.0

# S3
boto3>=1.35.0

# Cache
redis>=5.0.0

# Dev / tests
pytest>=8.0.0
ruff>=0.6.0
